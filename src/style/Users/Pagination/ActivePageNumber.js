import styled from "styled-components";
import PageNumber from "./PageNumber";

const ActivePageNumber = styled(PageNumber)`
  font-weight: bold;
  font-size: 120%;
`;

export default ActivePageNumber;
